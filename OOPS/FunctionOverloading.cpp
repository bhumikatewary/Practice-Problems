/* Write a program to compute absolute 
and average value for integer and
floating point numbers */

#include<iostream>
using namespace std;

class compute{
  public:
  int abs(int);
  float abs(float);
  float avg(int,float);
  float avg(int,int);
  float avg(float,float);
};
